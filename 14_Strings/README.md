```JAVA
// Strings
// strings is a built in class in Java

// there are 3 mathod for creating a strings
// String(char[])
// String(byte[])
// String(string)
public class Main
{
  public static void main (String[]args)
  {
    char c[] = { 'A', 'B', 'C', 'D' };
    String str1 = new String (c);
    // here we converted c array into a string

    byte b[] = { 65, 66, 67, 68 };
    String str2 = new String (b);
    // here we converted b array into a string

    String str2 = new String("Java");
    String str3= "Java"
    
	// here store java into pool and str3 is pointing it.


  }
}
```
 
