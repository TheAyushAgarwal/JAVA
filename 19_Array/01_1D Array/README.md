```JAVA
// Arrays
// 1D Array

// Array is the collection of similer data types
class HelloWorld {
    public static void main(String[] args) {
        int a[]={2,4,6,8,10};
        // every array in Java is created in Heap
        System.out.println(a[0]);
        
        // for acesseing the arrays for loops are used
        for(int i=0;i<a.length;i++)
        {
            System.out.println(A[i]);
        }
        // instead of using for loop we can use for each loop for accessing the array
        for(int x:a)// for every value of a
        {
            System.out.println(x);
            
        }
    }
}

```
 
